Problem Link:
https://codeforces.com/problemset/problem/1806/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        long long a, b, c, d;
        cin >> a >> b >> c >> d;

        long long r = d - b;
        if(r < 0) 
        {
            cout << -1 << "\n";
            continue;
        }

        long long l = r + a - c;
        if(l < 0) 
        {
            cout << -1 << "\n";
            continue;
        }

        cout << r + l << "\n";
    }
    return 0;
}
