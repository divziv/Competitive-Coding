Problem Link:
https://codeforces.com/problemset/problem/1312/B

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        int n;
        cin >> n;
        
        vector<int> a(n);
        for(int p = 0; p < n; p++) 
        {
            cin >> a[p];
        }
        
        sort(a.rbegin(), a.rend());
        
        for(int p = 0; p < n; p++) 
        {
            cout << a[p] << " ";
        }
        cout << "\n";
    }
    return 0;
}
