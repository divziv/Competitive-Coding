Problem Link:
https://codeforces.com/problemset/problem/2185/B

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while(t--) {
        int n;
        cin >> n;

        int m = 0;
        for(int i = 0; i < n; i++) 
        {
            int x;
            cin >> x;
            m = max(m, x);
        }

        cout << n * m << "\n";
    }

    return 0;
}
