Problem Link:
https://codeforces.com/problemset/problem/2013/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while(t--) {
        long long n, x, y;
        cin >> n >> x >> y;

        cout << (n + min(x, y) - 1) / min(x, y) << '\n';
    }

    return 0;
}
