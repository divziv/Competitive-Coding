Problem Link:
https://codeforces.com/problemset/problem/599/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    long long d1, d2, d3;
    cin >> d1 >> d2 >> d3;
    cout << min({2 * (d1 + d2), 2 * (d1 + d3), 2 * (d2 + d3), d1 + d2 + d3});
    return 0;
}
