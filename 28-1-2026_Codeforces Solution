Problem Link:
https://codeforces.com/problemset/problem/2037/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        int n;
        cin >> n;

        map<int, int> m;
        for(int i = 0; i < n; i++) 
        {
            int x;
            cin >> x;
            m[x]++;
        }

        int s = 0;
        for(auto &p : m) 
        {
            s += p.second / 2;
        }

        cout << s << "\n";
    }
    return 0;
}
