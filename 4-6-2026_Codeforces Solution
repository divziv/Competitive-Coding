Problem Link:
https://codeforces.com/problemset/problem/2148/B

Solution:
#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;

    while(t--)
    {
        int n, m, x, y;
        cin >> n >> m >> x >> y;

        int v;

        for(int i = 0; i < n; ++i)
        {
            cin >> v;
        }

        for(int i = 0; i < m; ++i)
        {
            cin >> v;
        }

        cout << n + m << '\n';
    }

    return 0;
}
