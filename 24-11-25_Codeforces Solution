Problem Link:
https://codeforces.com/problemset/problem/1858/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    long t;
    cin >> t;

    while(t--) {
        long a, b, c;
        cin >> a >> b >> c;

        if(a + (c % 2) > b)
            cout << "First\n";
        else
            cout << "Second\n";
    }

    return 0;
}
