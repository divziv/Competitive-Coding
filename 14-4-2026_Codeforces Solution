Problem Link:
https://codeforces.com/problemset/problem/1519/B

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while(t--) {
        int n, m, k;
        cin >> n >> m >> k;

        bool f = (n * m - 1 == k);
        cout << (f ? "YES" : "NO") << "\n";
    }

    return 0;
}
