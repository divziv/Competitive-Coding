Problem Link:
https://codeforces.com/problemset/problem/1873/D

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        int n, k;
        cin >> n >> k;
        string s;
        cin >> s;
        int o = 0;
        
        for(int i = 0; i < n; ) 
        {
            if(s[i] == 'B') 
            {
                o++;
                i += k;
            } 
            else 
            {
                i++;
            }
        }
        cout << o << "\n";
    }
    return 0;
}
