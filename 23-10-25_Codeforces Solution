Problem Link:
https://codeforces.com/problemset/problem/1843/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        int n;
        cin >> n;
        
        vector<long long> v(n);
        for(auto &x : v) 
            cin >> x;
            
        sort(v.begin(), v.end());

        long long s = 0;
        for(int i = 0; i < n / 2; i++) 
        {
            s += v[n - i - 1] - v[i];
        }

        cout << s << "\n";
    }
}
