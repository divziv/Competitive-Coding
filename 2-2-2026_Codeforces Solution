Problem Link:
https://codeforces.com/problemset/problem/2171/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        long n;
        cin >> n;
        
        if(n % 2) 
        {
            cout << 0 << "\n";
            continue;
        }
        n /= 2;
        cout << 1 + n / 2 << "\n";
    }

    return 0;
}
