Problem Link:
https://codeforces.com/problemset/problem/1311/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    long t;
    cin >> t;
    
    while(t--) {
        long a, b;
        cin >> a >> b;

        if(a == b) 
            cout << 0 << "\n";
        else if(b > a && (b - a) % 2 == 1) 
            cout << 1 << "\n";
        else if(b > a && (b - a) % 2 == 0) 
            cout << 2 << "\n";
        else if(a > b && (a - b) % 2 == 0) 
            cout << 1 << "\n";
        else if(a > b && (a - b) % 2 == 1) 
            cout << 2 << "\n";
    }
    return 0;
}
