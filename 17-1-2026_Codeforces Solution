Problem Link:
https://codeforces.com/problemset/problem/1828/B

Solution:
#include <bits/stdc++.h>
using namespace std;

int gcd(int a, int b) {
    return (b == 0) ? a : gcd(b, a % b);
}

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        int n;
        cin >> n;

        int g = 0;
        for(int i = 1; i <= n; i++) 
        {
            int p;
            cin >> p;
            
            int d = abs(i - p);
            g = gcd(g, d);
        }

        cout << g << "\n";
    }

    return 0;
}
