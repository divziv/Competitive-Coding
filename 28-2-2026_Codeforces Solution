Problem Link:
https://codeforces.com/problemset/problem/2123/B

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while(t--) {
        int n, j, k;
        cin >> n >> j >> k;

        int s = -1;
        int mx = -1;

        for(int i = 1; i <= n; i++) 
        {
            int x;
            cin >> x;

            if(i == j)
                s = x;

            mx = max(mx, x);
        }

        if(k == 1 && s < mx)
            cout << "NO\n";
        else
            cout << "YES\n";
    }

    return 0;
}
