Problem Link:
https://codeforces.com/problemset/problem/1471/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        long long n, x;
        cin >> n >> x;

        long long s = 0, mx = 0, mn;
        for(long long i = 0; i < n; i++) 
        {
            long long a;
            cin >> a;
            s += a;
            mx += (a + x - 1) / x;
        }

        mn = (s + x - 1) / x;
        cout << mn << " " << mx << "\n";
    }

    return 0;
}
