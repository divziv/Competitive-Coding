Problem Link:
https://codeforces.com/problemset/problem/2003/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main()
{
    long t;
    cin >> t;
    while(t--)
    {
        long n;
        cin >> n;
        string s;
        cin >> s;
        cout << (s[0] != s.back() ? "YES" : "NO") << '\n';
    }
}
