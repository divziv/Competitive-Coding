Problem Link:
https://codeforces.com/problemset/problem/1372/B

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) 
    {
        long long n;
        cin >> n;

        long long d = n;
        for(long long i = 2; i * i <= n; i++) 
        {
            if(n % i == 0) 
            {
                d = i;
                break;
            }
        }

        cout << n / d << " " << (n / d) * (d - 1) << '\n';
    }

    return 0;
}
