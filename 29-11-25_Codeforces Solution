Problem Link:
https://codeforces.com/problemset/problem/1872/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    long t;
    cin >> t;
    
    while(t--) 
    {
        long a, b, c;
        cin >> a >> b >> c;

        if(a > b) 
            swap(a, b);

        long d = (b - a + 1) / 2;
        long s = (d + c - 1) / c;

        cout << s << "\n";
    }

    return 0;
}
