Problem Link:
https://codeforces.com/problemset/problem/1420/B

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while(t--) {
        int n;
        cin >> n;

        vector<long long> v(31, 0);
        long long s = 0;

        for(int i = 0; i < n; i++) 
        {
            long long x;
            cin >> x;

            long long cnt = 31 - __builtin_clz(x);
            s += v[cnt];
            v[cnt]++;
        }

        cout << s << "\n";
    }

    return 0;
}
