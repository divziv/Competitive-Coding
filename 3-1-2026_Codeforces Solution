Problem Link:
https://codeforces.com/problemset/problem/2126/B

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    long t;
    cin >> t;
    
    while(t--) {
        long n, k;
        cin >> n >> k;

        bool b = 0;
        long z = 0, c = 0;

        for(long p = 0; p < n; p++) 
        {
            int a;
            cin >> a;

            if(a || b) 
            {
                b = 0;
                z = 0;
                continue;
            }

            ++z;
            if(z >= k) 
            {
                ++c;
                z = 0;
                b = 1;
            }
        }

        cout << c << "\n";
    }
    return 0;
}
