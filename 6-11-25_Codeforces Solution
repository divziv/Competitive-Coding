Problem Link:
https://codeforces.com/problemset/problem/1896/A

Solution:
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        int n;
        cin >> n;
        
        vector<int> v(n);
        for(int i = 0; i < n; ++i) 
        {
            cin >> a[i];
        }

        if(a[0] == 1) 
        {
            cout << "YES\n";
        } 
        else 
        {
            cout << "NO\n";
        }
    }
    return 0;
}
